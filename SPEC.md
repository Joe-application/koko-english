# ココイングリッシュ（KoKo English）実装仕様書

グアム旅行（2027年3月下旬〜4月上旬）に向けた、家族4人用の英会話練習 Web アプリ。
このファイルは Claude Code に渡す実装仕様書。**この文書に書かれていないライブラリ・サービスは追加しないこと。**

- プロジェクトルート: `~/Claude/GuamEnglish`
- 作成日: 2026-09-18 / 目標: 2027年3月中旬までに家族全員が Level 相当の会話をこなせる状態

---

## 1. 目的とユーザー

### 目的
グアム旅行で**現地の人と実際に英語で話せるようになる**こと。TOEIC 的な学習ではなく、
「入国審査で止められない」「レストランで自分で注文できる」「店員に話しかけられて固まらない」ための実地訓練アプリ。

### ユーザー（4人・同一世帯）
| ID | 想定 | レベル | 配慮 |
|---|---|---|---|
| 長男 | 小学6年 | **L1 (Easy)** | 日本語の指示は易しく、漢字にはふりがな相当の配慮。短文中心 |
| 長女 | 高校1年 | **L2 (Normal)** | 学校英語の知識あり。話す度胸をつけるのが主眼 |
| 夫 | 44歳 | **L3 (Hard)** | 読めるが話せない層。とっさの一言と聞き取りが主眼 |
| 妻 | 44歳 | **L3 (Hard)** | 同上 |

同じシナリオを **1本のコンテンツで3レベル分岐**させる（後述 §6）。人によってアプリが別物にならないようにする。

### 使い方の想定
- 1日1回、**5〜10分**。通学前／寝る前など。
- 通信は自宅 Wi-Fi 前提。ただし**グアム現地でオフラインでも開けること**（フレーズ帳として使うため）。

---

## 2. 確定済みの技術方針（変更禁止）

| 項目 | 決定 | 理由 |
|---|---|---|
| 形態 | **Web アプリ / PWA** | iPhone・Android 混在でも URL 1本で配れる。審査不要 |
| 配布 | **GitHub Pages**（public リポジトリ） | 無料・HTTPS（マイク利用に必須）・push で全員に反映 |
| 会話エンジン | **外部 API を一切使わない。全シナリオをアプリ内に内蔵** | 費用ゼロ・オフライン動作・子どもに出す内容を完全に制御できる |
| 音声 | **聞く（TTS）＋話す（STT）＋採点**、ブラウザ標準機能のみ | 追加費用ゼロ |
| データ | **端末内 localStorage のみ**。サーバー・アカウントなし | 個人情報を外に出さない |
| ゲーム要素 | **家族対戦（コードの手動共有）** と **コレクション** が主軸 | ユーザー選択による |
| キャラクター | **コードで描く SVG キャラ** | 画像素材が不要。表情・衣装を差し替えられる |

### 技術スタック
- **素の HTML / CSS / JavaScript（ES Modules）。ビルドツール・npm パッケージ・CDN 一切なし。**
  - 理由: GitHub Pages に push するだけで動く。ビルドが壊れて家族が使えない状態を作らない。
- フレームワークは使わない。画面切り替えは自前の軽量ルーター（hash ベース）。
- フォントは system font stack のみ（外部フォント読み込み禁止＝オフライン動作のため）。
- Service Worker で全アセットをキャッシュし、完全オフライン起動を可能にする。

---

## 3. ディレクトリ構成

```
GuamEnglish/
├── index.html
├── manifest.webmanifest
├── sw.js                     # Service Worker（全アセットを precache）
├── icons/                    # PWA アイコン（SVG から生成した PNG 192/512 を同梱）
├── css/
│   └── style.css
├── js/
│   ├── main.js               # 起動・ルーティング
│   ├── store.js              # localStorage 読み書き（唯一の永続化窓口）
│   ├── router.js
│   ├── speech/
│   │   ├── tts.js            # 読み上げ
│   │   ├── stt.js            # 音声認識（フォールバック判定を含む）
│   │   └── score.js          # 発話採点ロジック
│   ├── character/
│   │   └── koko.js           # SVG キャラの生成・表情・衣装
│   ├── views/
│   │   ├── home.js
│   │   ├── scenario.js       # 会話練習の本体
│   │   ├── result.js
│   │   ├── collection.js
│   │   ├── family.js         # 家族対戦（コード生成・読み込み）
│   │   ├── phrasebook.js     # 現地で使うフレーズ帳（オフライン）
│   │   └── settings.js
│   └── util/
│       ├── text.js           # 正規化・比較
│       └── code.js           # 家族対戦コードのエンコード/デコード
├── content/
│   ├── index.js              # 全シナリオの読み込みと順序定義
│   ├── scenarios/
│   │   ├── 01-airplane.js
│   │   ├── 02-immigration.js
│   │   └── ...               # §6 参照
│   ├── phrases.js            # フレーズ帳データ
│   └── items.js              # コレクション定義
└── SPEC.md                   # このファイル
```

コンテンツは **JSON ではなく `.js` の ES Module（`export default {...}`）** とする。
`fetch` を使わずモジュールグラフに含まれるため、読み込み失敗や Service Worker のキャッシュ漏れが起きない。

> 注: ES モジュールは `file://` では CORS によりブロックされる。開発中は必ずローカルサーバー経由で開くこと。
> `python3 -m http.server 8000` → `http://localhost:8000`

---

## 4. データモデル（localStorage）

キーは `kokoEnglish.v1` の 1 つのみ。JSON 文字列で保存。

```js
{
  version: 1,
  activeProfileId: "p1",
  profiles: [
    {
      id: "p1",
      name: "たろう",
      level: 1,                      // 1=Easy 2=Normal 3=Hard
      avatarColor: "#ffb703",
      createdAt: "2026-09-20",
      coins: 120,                    // アイテム交換用
      totalPoints: 860,              // 累計
      weekly: { weekId: "2026-W39", points: 140 },  // 家族対戦用。週が変わったら自動リセット
      lastPlayedDate: "2026-09-20",
      streak: 5,                     // 連続日数（家族対戦の週次集計に必要なので内部的に保持）
      scenarios: {
        "immigration": { cleared: true, bestScore: 92, plays: 3, lastPlayed: "2026-09-20" }
      },
      items: ["shell-key", "aloha-shirt"],   // 所持アイテム id
      equipped: { outfit: "aloha-shirt", hat: null },
      rivals: [                      // 他の家族から読み込んだスコア
        { name: "はなこ", weekId: "2026-W39", points: 210, importedAt: "2026-09-21" }
      ]
    }
  ],
  settings: {
    ttsRate: 0.9,                    // 読み上げ速度
    ttsVoiceURI: null,               // 選択した音声
    sttEnabled: true,                // 音声認識を使うか（オフ時は自己採点モード）
    seEnabled: true
  }
}
```

### 実装要件
- `store.js` 以外から `localStorage` を直接触らない。
- 書き込みは全て「読み込み → 変更 → 保存」を 1 関数にまとめ、**保存失敗（容量超過・プライベートモード）を握りつぶさず、画面上に警告を出す**。
- 端末を共有する場合に備え、プロフィールは最大 4 件まで作成・切り替えできる。
- `version` が将来上がったときのためにマイグレーション関数の入り口を用意しておく（中身は空でよい）。

---

## 5. 画面仕様

### 5.1 ホーム（`#/`）
- 上部: キャラ「ココ」の大きな SVG。装備中の衣装を反映。タップで一言しゃべる（TTS）。
- ココの吹き出し: 状況に応じたセリフ（初回、連続日数、週の残り日数、旅行までの残り日数）。
  - 例: 「グアムまであと **182日**！今日も1本いこう！」
- 「今日の練習」ボタン（未クリアの次のシナリオ、または復習をレコメンド）。
- 下部タブ: **ホーム / 練習 / コレクション / 家族 / フレーズ帳**。
- 右上にプロフィール切り替え（アイコン4つ）。

### 5.2 シナリオ一覧（`#/scenarios`）
- カード形式。各カードに アイコン・タイトル（日本語＋英語）・ベストスコア・クリア済みバッジ。
- 未解放シナリオは薄く表示し、解放条件を明記（例: 「入国審査をクリアすると解放」）。

### 5.3 シナリオ再生（`#/scenario/:id`）— **アプリの中核**
チャット風の縦スクロール UI。1ターンずつ進む。

- **NPC ターン**: 相手の吹き出しが出る → 自動で TTS 再生 → 「🔁 もう一度」「🐢 ゆっくり」ボタン。
  - 英文は**初期状態で表示**する（L1・L2）。L3 は初期状態で英文を隠し、「見る」で表示（聞き取り訓練）。
  - 日本語訳は常に「訳を見る」を押したときだけ表示。
- **ユーザーターン（mode: "speak"）**:
  1. 日本語で「何を言うか」の指示を表示（例: 「『予約しています』と伝えよう」）。
  2. 自分のレベルのお手本英文を表示（L3 は隠す→「ヒント」で表示）。
  3. 🎤 ボタンを押して話す → 認識結果と採点を表示（§7）。
  4. 80点未満は「もう一度」を促す。3回失敗したら「お手本を聞いて次へ」で進めるようにし、**絶対に詰まらせない**。
  5. **「もう一度話す」は押した瞬間に録音を開始する。** 結果画面からマイク待機画面に戻して🎤を押し直させない
     （1回の言い直しに2タップかかるのは、繰り返し練習するアプリでは致命的にストレスになる）。
     一度マイクの許可が通ったあとは、ボタンを押した時点ですぐ「聞いています」の表示に切り替える。
- **ユーザーターン（mode: "choice"）**: 3択。不正解時は「なぜ違うか」を1行で表示。
- 進行中はいつでも「中断」でき、途中経過は保存しない（次回は最初から。1本5分程度なので割り切る）。

### 5.4 リザルト（`#/result/:id`）
- スコア（100点満点）、獲得ポイント、獲得コイン、新規アイテム。
- ココが表情を変えて反応（90+ = 大喜び、70+ = 笑顔、それ未満 = 応援）。
- 「もう一度」「次のシナリオへ」「ホーム」。

### 5.5 コレクション（`#/collection`）
- **お土産**: 獲得済み／未獲得（シルエット表示＋獲得条件）。
- **衣装**: ココに着せ替え。タップで即反映しホームにも反映。
- **バッジ**: 実績（初クリア、全問正解、7日連続、全シナリオ制覇 など）。
- コイン残高と「ガチャではなく確定交換」のショップ（運要素は入れない。頑張った分だけ手に入る方が続く）。

### 5.6 家族（`#/family`）— 家族対戦
サーバーを使わずに競争させる仕組み。

- 自分の今週のポイントを大きく表示。
- **「コードを作る」**: 今週の成績を短い文字列に変換して表示。「コピー」ボタンで LINE に貼れる形にする。
  - コピーされるテキスト例:
    ```
    🏝 ココイングリッシュ 今週の成績
    たろう / 2026-W39 / 210pt
    ↓このコードをアプリの「家族」画面に貼ってね
    KE1.dGFyb3U.MjAyNi1XMzk.MjEw.a7
    ```
- **「コードを読み込む」**: テキスト入力欄に貼り付け → 家族のスコアとしてランキングに追加。
  - 同じ名前・同じ週のコードは上書き。
  - チェックサム不一致・週が古い場合はエラーメッセージを出す。
- **週間ランキング**: 自分＋読み込んだ家族を降順表示。1位に👑。
- 週の切り替わりは **月曜0:00（ローカル時刻）**。`weekId` は ISO 週番号形式 `YYYY-Www`。

#### コード仕様（`util/code.js`）
```
KE1.<base64url(name)>.<base64url(weekId)>.<base64url(points)>.<checksum>
```
- checksum: 前3フィールドの連結文字列に対する簡易ハッシュ（例: 各文字コードの総和 mod 1296 を36進数2桁）。
- 目的は改ざん防止ではなく**貼り付けミスの検出**。暗号強度は不要。
- 全体で60文字以内に収めること。

### 5.7 フレーズ帳（`#/phrasebook`）— 現地で実際に使う
- カテゴリ別（空港・ホテル・食事・買い物・ビーチ・トラブル・あいさつ）。
- 各フレーズ: 英文（大きく）／日本語／🔊読み上げ。
- **「見せて使う」モード**: フレーズをタップすると画面いっぱいに英文を表示。店員にそのまま画面を見せられる。
- 検索窓（日本語でも英語でもヒットする部分一致）。
- お気に入り登録（★）。★だけを表示するフィルタ。
- **この画面は完全オフラインで動くこと**（現地で通信がなくても使うため）。

### 5.8 設定（`#/settings`）
- プロフィール作成／編集／削除／切り替え。
- レベル変更。
- 読み上げ速度（0.7 / 0.85 / 1.0）、音声の選択（利用可能な en-US 音声の一覧から）。
- 音声認識 ON/OFF（OFF にすると自己採点モードになる）。
- **データのバックアップ**: 全データを JSON 文字列として表示＋コピー。復元用の貼り付け欄も用意。
  （端末を変えたりブラウザのデータが消えたときに救えるようにする）

---

## 6. 学習コンテンツ仕様

### 6.1 シナリオ一覧（旅程の順に並べる＝そのまま旅行の予習になる）

| # | id | タイトル | 主な内容 |
|---|---|---|---|
| 1 | `airplane` | 機内で | 飲み物を頼む、席を通してもらう、毛布を頼む |
| 2 | `immigration` | 入国審査 | 目的・滞在日数・滞在先・職業を答える |
| 3 | `taxi-hotel` | 空港からホテルへ | 行き先を伝える、所要時間・料金を聞く |
| 4 | `hotel-checkin` | ホテルでチェックイン | 予約確認、朝食の時間、Wi-Fi パスワード |
| 5 | `restaurant` | レストランで | 人数を伝える、注文、おすすめを聞く、会計 |
| 6 | `fastfood` | ファストフード・カフェ | セット注文、店内/持ち帰り、カスタマイズ |
| 7 | `shopping` | ABCストア・買い物 | 探しているものを聞く、試着、支払い方法 |
| 8 | `beach` | ビーチ・アクティビティ受付 | 予約、レンタル、時間の確認、安全の確認 |
| 9 | `directions` | 道をたずねる | 場所を聞く、聞き返す、お礼 |
| 10 | `trouble` | 困ったとき | 体調不良、落とし物、部屋のトラブル、聞き返す |
| 11 | `smalltalk` | 現地の人と雑談 | どこから来たの、何日いるの、グアムはどう？ |
| 12 | `souvenir` | お土産・帰りの空港 | 値段交渉なしの買い物、免税、チェックイン |

- 各シナリオは **8〜12ターン**、所要 4〜6分。
- 解放順は上記の通り。`airplane` のみ最初から解放。
- **聞き返しの練習を全シナリオに1回以上必ず入れる**（`Sorry, could you say that again?` / `Could you speak more slowly?`）。
  旅行で最も使う技能であり、これができれば会話が止まらないため。

### 6.2 シナリオのデータ形式

```js
// content/scenarios/04-hotel-checkin.js
export default {
  id: "hotel-checkin",
  order: 4,
  title: { ja: "ホテルでチェックイン", en: "Hotel Check-in" },
  icon: "🏨",
  place: "Tumon, Guam",
  unlockAfter: ["taxi-hotel"],        // 空配列なら最初から解放
  reward: { coins: 30, item: "shell-key" },
  intro: {
    ja: "ホテルに着いた。フロントで名前を伝えてチェックインしよう。",
    tips: ["名前は聞き取りにくいので、ゆっくりハッキリ言うのがコツ。"]
  },
  turns: [
    {
      speaker: "npc",
      en: "Hafa adai! Welcome to the Pacific Bay Hotel. Are you checking in?",
      ja: "ハファデイ！パシフィックベイホテルへようこそ。チェックインですか？",
      note: "Hafa adai（ハファデイ）はチャモロ語の「こんにちは」。グアムでよく使われる挨拶。"
    },
    {
      speaker: "user",
      mode: "speak",
      instruction: { ja: "「はい。ジョウズカの名前で予約しています」と伝えよう" },
      variants: {
        1: "Yes. Jozuka.",
        2: "Yes, I have a reservation. Jozuka.",
        3: "Yes, I have a reservation under the name Jozuka."
      },
      keywords: ["reservation"],        // 採点時に必須の語（§7.3）。L1 では keywords を無視
      hint: { ja: "under the name 〜 = 〜という名前で" }
    },
    {
      speaker: "npc",
      en: "Thank you. May I see your passport, please?",
      ja: "ありがとうございます。パスポートを見せていただけますか？"
    },
    {
      speaker: "user",
      mode: "choice",
      instruction: { ja: "「はい、どうぞ」はどれ？" },
      options: [
        { en: "Here you are.", correct: true },
        { en: "Here I am.", correct: false, why: "「ここにいます」という意味になってしまう" },
        { en: "Take it.", correct: false, why: "命令口調でぶっきらぼうに聞こえる" }
      ]
    }
    // ... 以降 8〜12 ターンまで
  ],
  wrapUp: {
    ja: "チェックインで使う3つ: reservation / Here you are. / What time is breakfast?",
    phrases: ["I have a reservation.", "Here you are.", "What time does breakfast start?"]
  }
}
```

### 6.3 ターンの `mode`
| mode | 内容 |
|---|---|
| `speak` | マイクで発話 → 採点。メインの練習 |
| `choice` | 3択。文法・語感を学ぶ。1シナリオに2〜3回 |
| `listen` | NPC の発話を聞いて内容を3択で答える（聞き取り専用）。1シナリオに1〜2回 |

### 6.4 レベル分岐のルール
- `variants` は 1/2/3 の3種を**必ず全て書く**。
- L1 は「通じる最短の言い方」。文法的に完璧でなくてよいが、失礼にならないこと。
- L3 は「大人として自然な言い方」。丁寧さのニュアンスを含む。
- 表示・採点はプロフィールの `level` の variant のみを使う。
- 設定でレベルを変えたら、既存の記録は保持したまま次回から新しい variant を使う。

### 6.5 英語の内容に関する注意
- **グアム＝アメリカ領**なので、アメリカ英語（米つづり・米語彙）で統一する。
- 実在ホテル名・店名は使わない。架空の名前（Pacific Bay Hotel 等）にする。
- チャモロ語のあいさつ `Hafa adai` は 1〜2 箇所で紹介する程度にとどめ、学習の主軸は英語にする。
- 子どもが使うので、スラング・飲酒・チップ交渉の駆け引きなどは扱わない。チップは「レストランで伝票にどう書くか」を事実として1回説明する程度。

---

## 7. 音声仕様（実装上いちばん壊れやすい箇所）

### 7.1 読み上げ（TTS）— `js/speech/tts.js`
`window.speechSynthesis` を使用。

**必ず守る実装ルール:**
1. **iOS Safari は音声リストが非同期で届く。** `speechSynthesis.getVoices()` が空配列を返す前提で、`voiceschanged` イベントを待つ実装にする。
2. **iOS は最初の発話がユーザー操作（タップ）起点でないと鳴らない。** アプリ起動後、最初のユーザータップ時に無音の短い発話を1回流して「解錠」しておく。
3. `speak()` の前に必ず `speechSynthesis.cancel()` を呼ぶ（iOS でキューが詰まって無音になる既知の挙動への対策）。
4. **音声選びは「en-US の最初の1つ」にしてはいけない。**
   macOS / iOS は Albert・Zarvox・Bubbles・Bad News といったジョーク用の音声を同じ一覧に混ぜてくる。
   名前順で最初に来るのが Albert（しゃがれた不気味な声）なので、素直に選ぶと教材として成立しない。
   - ジョーク音声の名前リストを持ち、**除外する**。
   - 聞き取りやすい音声（Samantha / Ava / Allison / Alex / Google US English / Microsoft Aria …）を名前で優先する。
   - それも無ければ `default` の音声 → en-US のもの → 英語のもの、の順。
   - 教材用とココ用でそれぞれ設定画面から音声を選べるようにし、選んだらその場で試聴させる。
5. 「🐢 ゆっくり」は `rate` を設定値の 0.65 倍にして再生。
6. TTS が利用できない端末では、読み上げボタンを非表示にし、英文テキストを常時表示に切り替える（無言で壊れない）。
7. **ココの声は教材の読み上げと分ける。** 同じ声・低いピッチ・遅い速度だと不気味に聞こえるため、
   ココのセリフ専用に `speakKoko()` を用意し、`pitch` を上げ（既定 1.55）`rate` は 1.0 にして、
   子どもっぽく親しみやすい声にする。明るく聞こえる音声（Samantha / Karen 等）を優先して選ぶ。
   ピッチは設定画面から「ひくめ / ふつう / とても高い」で変更できること。
   **教材（NPC のセリフ・お手本の英文）はピッチを変えない。** 学習の見本なので自然な発音を保つ。

### 7.2 音声認識（STT）— `js/speech/stt.js`
`window.SpeechRecognition || window.webkitSpeechRecognition` を使用。

**重要な前提（仕様書として明示しておく）:**
> ブラウザの音声認識は多くの環境で**サーバー側で処理されるため通信が必要**。
> また iOS Safari ではホーム画面に追加した PWA（standalone 表示）で音声認識が動作しない場合がある。
> よって **音声認識は「あれば使う」機能として実装し、使えないときに必ず代替手段へ倒す**こと。

**必ず守る実装ルール:**
1. 起動時に対応可否を判定し、非対応なら設定の `sttEnabled` を強制的に OFF にして**自己採点モード**へ。
2. 認識設定: `lang = "en-US"`, `interimResults = true`, `continuous = false`, `maxAlternatives = 3`。
3. 採点は `maxAlternatives` の**どれか1つでも高スコアならその最高点を採用**する（認識の揺れで理不尽に減点しないため）。
4. タイムアウト: 🎤 を押してから 8秒 で自動停止。無音のまま終了したら「聞こえなかったみたい。もう一度話してみて」と表示し、**減点しない**。
5. `onerror` の扱い:
   - `not-allowed` / `service-not-allowed` → マイク許可の案内を表示し、自己採点モードへ切り替えるボタンを出す。
   - `network` → 「ネットにつながっていないと採点できないよ」と表示し、自己採点モードへ。
   - `no-speech` / `aborted` → エラー表示せず、もう一度促すだけ。
6. 認識中は録音インジケータ（波形風の CSS アニメ）を出す。ココも「聞いているポーズ」にする。
7. **マイクの使用許可を明示的に求める導線を用意する。**
   - 設定画面に「マイクの使用を許可する」ボタンと、現在の許可状態（許可済み / ブロック中 / 未許可）の表示を置く。
   - 許可の要求は `navigator.mediaDevices.getUserMedia({audio:true})` で行い、確認できたらトラックをすぐ停止する。
   - 状態の確認は `navigator.permissions.query({name:"microphone"})`。非対応ブラウザでは "unknown" として扱う。
   - 練習中に🎤を押したとき未許可なら、その場で許可を求める（必ずタップ起点で呼ぶこと）。
   - 拒否された場合は日本語で理由を伝え、そのまま自己採点モードで続行できるようにする。

### 7.3 採点ロジック — `js/speech/score.js`
入力: 認識結果テキスト（複数候補）、正解文（そのレベルの variant）、`keywords`。

```
1. 正規化: 小文字化 / 記号除去 / 短縮形展開 (I'm→i am, don't→do not) /
          数字の表記ゆれ吸収 (2→two) / 冠詞 a,an,the は比較から除外
2. 単語配列にして LCS（最長共通部分列）を取る
3. 一致率 = LCS長 / max(正解語数, 発話語数)   → 0.0〜1.0
4. keywords が全て含まれていれば +0.1（上限1.0）。1つでも欠けていれば ×0.8
   ※ level 1 のときは keywords 判定を行わない（子どもを必要以上に落とさない）
5. スコア = round(一致率 × 100)
```

**フィードバック表示:**
- 90点以上「Perfect! 完璧！」／70〜89「Good! 通じるよ」／50〜69「もう少し！」／50未満「もう一度いこう」
- 正解文と発話文を並べ、**足りなかった語を黄色、余分だった語をグレー**で表示する。
- 認識は完璧ではないので、「発音が悪い」とは決して言わない。「うまく聞き取れなかったかも」という言い回しにする。

### 7.3.5 端末チェック（設定画面）
音声まわりは「対応しているのに鳴らない」ことが多く、原因が端末ごとに違う。
家族4人の別々の端末で使うため、**本人が原因を切り分けられる画面**を設定に置く。
ボタン1つで以下を順に判定し、それぞれ ✅ / ⚠️ と日本語の対処法を出す。

| 項目 | 判定方法 |
|---|---|
| 読み上げ | 実際に短い発話を投げ、2秒以内に `onstart` が来るかを見る。来なければ「音声は用意できたが再生が始まらない」と案内する |
| 接続の安全性 | `window.isSecureContext`。false なら「https か localhost で開かないとマイクは使えない」と明示する |
| 音声認識 | `SpeechRecognition` の有無 |
| マイクの許可 | `navigator.permissions.query({name:"microphone"})` |
| インターネット | `navigator.onLine` |

> 「対応しているか」だけを見て OK と表示してはいけない。
> `speechSynthesis` は対応していても無音のままキューに積まれることがあるので、**実際に鳴り始めたかを確認する**こと。

### 7.4 自己採点モード（STT が使えない場合の代替）
1. お手本を TTS で聞く。
2. `MediaRecorder` で自分の声を録音（マイク許可が取れる場合）。取れなければ録音なしで進める。
3. 録音を聞き返す → 「言えた ◎ / まあまあ ○ / むずかしい △」を自分で選ぶ。
4. ◎=90点、○=70点、△=40点として記録する。
5. この経路でも**ポイント・コイン・アイテムは通常どおり獲得できる**こと（家族間で不公平にしない）。

---

## 8. ゲーム要素

### 8.1 ポイントとコイン
- シナリオ1本クリア: `基礎30pt + スコア平均 × 0.5`（端数切り捨て）。
- 同じシナリオの2回目以降は **獲得ポイント50%**（周回で荒稼ぎさせない）。
- コインはポイントの 1/3。
- `weekly.points` は毎週月曜にリセット（家族対戦用）。`totalPoints` は累計で減らない。

### 8.2 コレクション — `content/items.js`
3種類。すべて **SVG をコードで描画**する（画像ファイルなし）。

```js
export const items = [
  { id: "shell-key",  type: "souvenir", name: "貝がらのキーホルダー", how: "ホテルでチェックインをクリア" },
  { id: "aloha-shirt",type: "outfit",   name: "アロハシャツ",         how: "ショップで 60コイン" , cost: 60 },
  { id: "first-win",  type: "badge",    name: "はじめの一歩",         how: "最初のシナリオをクリア" },
  ...
]
```
- **お土産（souvenir）12種**: 各シナリオのクリア報酬。ラッテストーン、ヤシの実、シュノーケル、かき氷、絵はがき 等。
- **衣装（outfit）8種**: コインで交換。アロハシャツ、サングラス、麦わら帽子、浮き輪、リュック 等。ココに装備できる。
- **バッジ（badge）10種**: 実績。初クリア／全問90点以上／7日連続／全シナリオ制覇／フレーズ帳★10個／家族1位 等。
- 未獲得は**シルエット＋獲得条件**を必ず表示する（何をすれば手に入るか分からない状態を作らない）。
- ショップは**確定交換のみ。ガチャ・ランダム要素は入れない。**

### 8.3 家族対戦
- §5.6 のコード共有方式。
- ランキングは**今週のポイントのみ**で競う（累計だと大人が有利になり子どもが追いつけないため）。
- 1位のプロフィールには翌週 👑 アイコンが付く。
- 煽る文言は入れない。ココのコメントは「みんな頑張ってるね！」程度にとどめる。

### 8.4 継続の仕掛け（軽め）
※「連続日数」は主軸に据えないが、週次集計に必要なので内部で保持し、ホームに控えめに1行表示する。
- ホームに「グアムまであと N 日」を常に表示（これが最大の動機づけになる）。
- 3日以上空いたら、ココが責めずに迎える（「おかえり！ゆっくりでいいよ」）。**連続記録を失った演出は出さない。**

---

## 9. キャラクター仕様 — 「ココ」

- **設定**: グアムに実際にいる飛べない鳥「ココ（ko'ko'、グアムクイナ）」がモチーフ。
  英語を教えてくれる、のんびりした案内役。日本語で話しかけ、英語のお手本は英語で言う。
- **実装**: `js/character/koko.js` が SVG 文字列を返す関数を提供する。
  ```js
  renderKoko({ expression: "happy" | "normal" | "listening" | "cheer" | "surprised" | "sleepy",
               outfit: "aloha-shirt" | null,
               hat: "straw-hat" | null,
               size: 160 })
  ```
- 目・くちばし・体をパーツ分けし、**表情は目と口の `path` 差し替えだけで切り替える**。
- CSS アニメーション: まばたき（4〜7秒ごとにランダム）、待機時の上下ゆれ、正解時のジャンプ。
- 衣装・帽子は本体 SVG の上に重ねるレイヤーとして描く（装備なしでも成立する構造にする）。
- **`prefers-reduced-motion` を尊重し、指定時はアニメーションを止める。**
- セリフは `character/lines.js` にまとめ、状況キー（`onHome` / `onCorrect` / `onWrong` / `onComeback` / `onLevelUp`）ごとに複数用意してランダムに選ぶ。同じセリフの連続は避ける。

---

## 10. デザイン要件

- **モバイル縦持ち専用**。デスクトップでは中央に幅 480px で表示。
- トーン: グアムの海と空。ターコイズ `#2ec4b6`、サンドベージュ `#fdf6e3`、サンセットオレンジ `#ff9f1c`、濃紺 `#14213d`。
- タップ領域は最小 48×48px。小6も44歳も押しやすいサイズ。
- 英文は 20px 以上、日本語の補足は 14px。
- ダークモード対応（`prefers-color-scheme`）。CSS 変数で色を定義し、`:root` と `@media (prefers-color-scheme: dark)` で切り替える。
- `viewport-fit=cover` と `env(safe-area-inset-*)` で iPhone のノッチ・ホームバーを回避する。
- 効果音は Web Audio API で短いビープを合成して鳴らす（音声ファイルを持たない）。設定でオフ可。

---

## 11. PWA・オフライン

- `manifest.webmanifest`: `name`「ココイングリッシュ」、`short_name`「ココ英語」、`display: "standalone"`、`start_url: "./"`、`scope: "./"`、`theme_color: "#2ec4b6"`、アイコン 192/512。
- **パスは全て相対パス**にする（GitHub Pages のサブディレクトリ配信で壊れないため）。
- `sw.js`: install 時に全アセットを precache、fetch は cache-first。
  - `CACHE_NAME` にバージョン番号を持たせ、activate で古いキャッシュを削除する。
  - 更新時に家族の端末が古いまま固まらないよう、新しい SW を検知したら画面上部に「新しいバージョンがあります [更新]」バーを出す。
- **オフラインで必ず動く**: ホーム、シナリオ（選択式・自己採点）、コレクション、フレーズ帳、家族画面。
  音声認識だけはオフラインで動かないので、オフライン検知時は自動で自己採点モードに切り替え、その旨を1行表示する。

---

## 12. 実装フェーズ

各フェーズの終わりに動く状態にすること。全部できてから動く、という進め方はしない。

**Phase 1 — 骨格** ✅ 実装済み（2026-09-18）
- HTML/CSS/ルーター、`store.js`、プロフィール作成と切り替え
- シナリオ再生の基本（NPC 表示 + TTS + `choice` モード）
- シナリオ 2本（`airplane`, `immigration`）を完成させる
- リザルト画面、ポイント保存
- → この時点で iPhone 実機のブラウザで一通り動くことを確認する（**未実施**: 実機確認が残っている）

**Phase 2 — 話す** ✅ 実装済み（2026-09-18）
- STT 実装＋採点＋フィードバック表示
- 自己採点モードのフォールバック一式
- `speak` / `listen` モードをシナリオに組み込む

**Phase 3 — キャラとコレクション**
- ココの SVG、表情、アニメ、セリフ
- アイテム／衣装／バッジ、ショップ、コレクション画面

**Phase 4 — 家族とフレーズ帳**
- 家族対戦コードの生成・読み込み・ランキング
- フレーズ帳（「見せて使う」モード含む）

**Phase 5 — 残りのコンテンツと配信**
- シナリオを全12本に拡充
- PWA / Service Worker / アイコン
- GitHub Pages への配信手順を `README.md` に記載

---

## 13. 受け入れ基準

実装完了と言えるのは、以下が全て満たされたとき。

1. iPhone の Safari と Android の Chrome の両方で、URL を開いて最初のシナリオを最後まで完了できる。
2. ホーム画面に追加した状態（standalone）でも起動し、**機内モードでもフレーズ帳とシナリオが使える**。
3. 音声認識が使えない環境でも、自己採点モードで最後まで進めてポイントが入る。
4. レベル1とレベル3で、同じシナリオが別の英文で出題される。
5. ブラウザを再起動しても記録が残る。プロフィールを切り替えると記録も切り替わる。
6. 家族対戦コードを生成 → 別の端末（または別プロフィール）に貼り付け → ランキングに反映される。
7. 1文字も打たずにスコアが伸びる操作（連打・空タップ）で不正にポイントが増えない。
8. JavaScript エラーがコンソールに1つも出ない状態で、全画面を一巡できる。
9. 外部ネットワークへのリクエストが**一切発生しない**（DevTools の Network で確認。ブラウザ内蔵の音声認識を除く）。

---

## 14. 実装時の禁止事項・注意

- **npm パッケージ・CDN・外部フォント・外部 API を追加しない。** 必要だと感じたら、まず自前で書けないか検討し、それでも必要なら理由とともに相談する。
- **画像ファイル（png/jpg）を使わない。** アイコンは SVG かコードで描く（PWA アイコンだけは例外的に PNG を同梱可）。
- localStorage のキーを増やさない（`kokoEnglish.v1` の1つに集約）。
- 学習記録を削除する操作には必ず確認ダイアログを入れる。
- 英文は**必ず自然な英語であること**。不自然な和製英語を教材にしない。迷ったら簡単な言い方を選ぶ。
- 子どもが使うので、間違いを責める表現・煽る表現・「ダメ」「違う」といった強い否定語をUIに使わない。

---

## 15. 将来の拡張（今は作らない。作りやすさだけ残しておく）

- 家族スコアのサーバー共有（`family.js` のデータ取得部分を差し替えれば済む構造にしておく）
- シナリオの自由会話モード（LLM API 接続。`speech/` と `views/scenario.js` の境界を明確に保つ）
- 旅行から帰ったあとの「実際に使えたフレーズ」記録モード
